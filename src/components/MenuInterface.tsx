import { MenuContainer, MenuItem, MenuItemSelected } from "./Interface";
import React from 'react'

export const MilestonesInterface = () => {
    return (
        <MenuContainer>
            <MenuItem></MenuItem>
            <MenuItemSelected></MenuItemSelected>
        </MenuContainer>
    );
}
